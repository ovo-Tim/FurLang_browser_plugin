name: Plugin Build
on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release Tag'
        required: true
        type: string

jobs:
  create:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Release
        run: |
          gh release create ${{ github.event.inputs.release_tag }} --title "Release ${{ github.event.inputs.release_tag }}" \
          --notes "Action auto release" --draft
        env:
          GH_TOKEN: ${{ github.token }}

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    permissions:
      contents: write
    needs: create

    steps:
      # Check-out repository
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install Dependencies
        run: |
          pnpm install -g @vue/cli
          pnpm install -g @vue/cli-service
          pnpm install -g @vue/cli-plugin-babel
          pnpm install -g @vue/cli-plugin-eslint
          pnpm install -g @vue/cli-plugin-unit-jest
          pnpm install -g @vue/cli-plugin-vuex
          pnpm install -g @vue/cli-plugin-typescript
          pnpm install -g vue-cli-plugin-mdb5
          pnpm install

      - name: Build
        run: |
          pnpm build_panel
          pnpm zip
          pnpm zip:firefox

      - name: Release
        run: |
          gh release upload ${{ github.event.inputs.release_tag }} ./.output/*.zip
        env:
          GH_TOKEN: ${{ github.token }}
